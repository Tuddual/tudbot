const setup = require("./setup");
const setprefix = require("./setprefix");
const setcolor = require("./setcolor");

module.exports = {
    "setup": setup,
    "setprefix": setprefix,
    "prefix": setprefix,
    "setcolor": setcolor,
    "color": setcolor
}